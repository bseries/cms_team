tabelle erstellen im mysql -> an den anderen ausrichten
readme direkt kopieren inhalt banner durch team ersetzten etc.
composer.json erstmal kopieren dann anpassungen vornehmen sollte eigentlich schon stimmen -> oben titel anpassen
config -> panes.php hier geht es um den admin bereich -> reiter im cms erstellen 
Header in panes.php anpassen _> datum entspricht startdatum
panes ist die klasse mit der man registerkarten im cms unter content hinzufügen kann
g11n/message 
http://li3.me/docs/manual/common-tasks/globalization.md -> using localized content
extract ist lithium spezifisch $t steht für translate 
$t('zu übersetzender') in views wenn was über poedit übersetzten werden soll.
config /widges.php muss ich erstmal nicht anlegen. auf dem dashbaord im cms statistiken - brauchen wir nicht.

Traits cms UI funktionalitäten sind die man mit use im bannerControllers geht nur ums cms dort mit use alles verwenden
alles was man den selectTrait  übernimmt irgendwie die ausgabe einer funktion direkt ???  
unter modueles/banner belongs to erstmal copy und pastoi
chatham/app/composer.json
TRELLO UNKLARHEITEN
- "Model heißt TeamMembers" -> was heißt das cms_team statt cms_banners aber TeamMembersController statt BannersController? oder nur model/teammembers statt model/banners ?
- groß und kleinschreibungen z.b. bei panes register>controller>teamMembers ??? Konventionen unklar
- im cms oben wenn man links oben steht List Posts oder List Team Members -> woher kommt die groß und kleinschreibung bei posts? wenn ich team members klein schreibe wird es auch bei List team members klein sein. beim view von cms post ist das anders. da wird es automatisch groß 
in views/form.html.php woher kommt $this in zeile 10 und was macht die set funktion? woher kommt $item?
was hat es mit page => single oder multiple auf sich? 
was ist ni models/model.php mit belongsTo ? CoverMedia wird PortraitMedia oder was?
pagination funktioniert nicht in admin_index.html.php
groß und kleinschreibung im config/panes. controller= > teamMembers oder TeamMembers?
wie funktioniertniert bei view/index die sortable funktion ( order id verändert sich nicht wenn ich manuell sortiere)


